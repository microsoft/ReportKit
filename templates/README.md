# Templates

Each directory contains a versioned capability contract, design notes, and the preserved HTML
prototype. The prototypes are not yet connected to the deterministic generator.

Shared design primitives will live under `_shared` and must be reused by all templates.

